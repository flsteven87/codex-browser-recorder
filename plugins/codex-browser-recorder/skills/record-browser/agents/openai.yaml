interface:
  display_name: "Record Browser"
  short_description: "Run the approved Browser recording integration gate"
  default_prompt: "Use $record-browser to run the approved integration gate."
policy:
  allow_implicit_invocation: false
