interface:
  display_name: "Record Browser"
  short_description: "Record one approved Browser tab to local WebM"
  default_prompt: "Use $record-browser to record the approved Browser tab."
policy:
  allow_implicit_invocation: false
